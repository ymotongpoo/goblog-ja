box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
        theme: nofancy
        flags: --disableSitemap=true --baseUrl=https://ymotongpoo.github.io/goblog-ja/
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
