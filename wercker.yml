box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: angels-ladder
        flags: --disableSitemap=false --disableRSS=false --baseUrl=https://ymotongpoo.github.io/goblog-ja/
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
